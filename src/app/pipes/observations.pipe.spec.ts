import { ObservationsPipe } from "./observations.pipe";

describe("ObservationsPipe", () => {
  it("create an instance", () => {
    const pipe = new ObservationsPipe();
    expect(pipe).toBeTruthy();
  });
});
